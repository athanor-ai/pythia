.lake/
lake-manifest.json.lock
*.olean
build/
# Aristotle / prover-run artefacts
*-aristotle.tar.gz
aristotle_out/
*_out/
*_out.tar.gz
project_aristotle/

# Built distribution artifacts (sdist + wheel)
python/dist/
python/build/
python/*.egg-info/

.claude/

# Stale scratch directories from past experiments (not source).
Kairos/
# Python tooling artifacts (pytest + coverage).
__pycache__/
*.pyc
.coverage
.coverage.*
coverage.xml
.pytest_cache/

# Aristotle queue state — per-developer / per-account project ids,
# do not commit. See tools/aristotle_queue.py.
.aristotle/

# Markdown-doctest scratch tempfiles. The harness writes per-block
# tempfiles here and unlinks them on exit, but a crashed run can
# leave .lean files behind. Source-controlled scratch namespaces
# (SwarmDogfood/, SDKFleetDemo/) are NOT covered by this rule.
Pythia/Scratch/MdDoctest/
.pythia/

# Rust build artifacts
target/

